// No includes, they may the output harder to read

#define min(X, Y)  ((X) < (Y) ? (X) : (Y))

#define PI 3.14

int main() 
{

    float i = PI;

    float j = 2.71;

    float k = min(i,j);

    return 0;
}
